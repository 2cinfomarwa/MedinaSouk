{% extends 'layout_FrontEnd.html.twig' %}

{% block Content %}
    <h1>Media list</h1>

    <table>
        <thead>
            <tr>
                <th>Designation</th>
                <th>Type</th>
                <th>Path</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for medium in media %}
            <tr>
                <td><a href="{{ path('media_show', { 'id': medium.id }) }}">{{ medium.designation }}</a></td>
                <td>{{ medium.type }}</td>
                <td>{{ medium.path }}</td>
                <td>{{ medium.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('media_show', { 'id': medium.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('media_edit', { 'id': medium.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('media_new') }}">Create a new medium</a>
        </li>
    </ul>
{% endblock %}
