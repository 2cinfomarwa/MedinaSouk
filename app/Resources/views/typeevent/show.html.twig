{% extends 'layout_FrontEnd.html.twig' %}

{% block Content %}
    <h1>Typeevent</h1>

    <table>
        <tbody>
            <tr>
                <th>Libelle</th>
                <td>{{ typeevent.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ typeevent.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ typeevent.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typeevent_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typeevent_edit', { 'id': typeevent.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
