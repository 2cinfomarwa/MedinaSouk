{% extends 'layout_BackEnd.html.twig' %}

{% block Content %}
    <h1>Veuillez ajouter une catégorie</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('event_backend_listetype') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
